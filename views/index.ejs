<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>khatabook</title>
    <link rel="stylesheet" href="/css/output.css">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet"/>
</head>
<body>
    <div class="h-screen w-screen bg-zinc-800 text-zinc-200 ">
       <div class="flex justify-between px-5 items-center">
       <h3 class="text-2xl">Khatabook</h3>
        <div class="flex gap-4 text-xl">
            <a href="/">Home</a>
            <a href="/create">Create New</a>

        </div>
       </div>
       <h1 class="text-3xl my-5">All hisaab kitaab</h1>
        <% if(files.length > 0) { %>
              <% files.forEach((file) => { %>
                 <div class="flex h-10 w-[320px] bg-blue-500 my-10 rounded gap-20 items-center justify-center">
                    <div class="flex gap-4 items-center">
                        <h3><%= file.split(".")[0] %></h3>
                       <a href="/hisaab/<%= file %>"> <i class="ri-arrow-right-line text-white"></i></a>   
                 </div>
                
                  <div class="flex gap-2 item">

                  <a href="/edit/<%= file %>"><i class="ri-pencil-line text-xl"></i></a>    
                  <a href="/delete/<%= file %>"><i class="ri-delete-bin-line text-red-300 text-xl"></i></a>  
                 </div>
                 </div> 
      
            <%  }) %>

        <% } else{ %>
             <h3 class="text-zinc-500">Currently nothing to show , create hisaab to see something.</h3>
         <% } %>
        </div>
    </div>
    
</body>
</html>